# KrishiMitra AI — dependencies
# Core web/api
fastapi==0.118.0
uvicorn[standard]==0.37.0
python-multipart==0.0.20

# UI
streamlit==1.63.0

# Config / validation
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# LLM
openai==2.7.1

# Embeddings / ML
sentence-transformers==6.0.1
torch==2.14.0
scikit-learn==1.9.0
numpy==2.5.3

# Vector store
chromadb==1.5.9

# Knowledge graph
networkx==3.6.1

# Lexical retrieval (hybrid search alongside vector search)
rank-bm25==0.2.2

# Document processing
pymupdf==1.26.6
pypdf==6.4.0
python-docx==1.2.0
openpyxl==3.1.5
pandas==2.3.3
pyarrow==25.0.1
Pillow==11.3.0
pytesseract==0.3.13

# Web crawling
playwright==1.56.0

# Optional live web search (app/websearch/) — HTML parsing only, no API key
beautifulsoup4==4.13.4

# Utilities
PyYAML==6.0.3
requests==2.34.2
tenacity==9.1.4

# Optional supplementary guardrail/eval layers (pluggable — the app works
# fully without them; see app/guardrails/nemo_adapter.py and
# app/evaluation/deepeval_runner.py for the fallback behaviour).
nemoguardrails==0.17.0
deepeval==4.2.2

# Testing
pytest==8.4.2
httpx==0.28.1
